# 网页与小程序流程对齐实施计划

**目标：** 保留 Token 登录与 Next.js 后端，删除历史批量提交和生成运动数据；缺少小程序必要运行能力时阻止开跑。

**已确认约束：** 用户选择保留网页、接受平台能力限制。当前网页没有等价的微信后台定位及运动分析采集层，不能声称完整复刻小程序。

**实施方式：** 在当前分支内顺序执行，不创建新项目或部署。

- [x] 删除记录页补跑队列、日期选择、批量 API 及辅助代码。
- [x] 删除一键提交、人工里程／用时／日期输入和轨迹生成器；旧提交 API 返回 410，不请求上游。
- [x] 保留任务和路线浏览、成绩查询；页面显式显示开跑能力限制，禁止开始跑步。
- [x] 删除无效状态、公告入口及生成逻辑测试；保留协议与登录测试，补充旧接口禁用测试。
- [x] 记录逆向证据和未对齐项，避免将未实现的运动流程描述为已完成。
- [x] 运行测试、lint、构建和桌面／手机浏览器检查，确认没有任何运动写入请求。

## 逆向证据与边界

依据 `security-audit/wx8e8598deed63f9b1/analysis/chunk_96.appservice.formatted.js`：

| 环节 | 源码位置／行为 | 当前网页处理 |
| --- | --- | --- |
| 任务 | 1868 行 initSunRunLine，getSunrunPaper，首任务＋最近路线 | 保留任务／路线浏览；未采集位置，不伪称已选择最近路线 |
| 设备与开跑 | 1973 行 checkStartRun，设备限制、任务日期、当前时段、GCJ-02 位置距路线 100 米 | 运行能力不足时整体阻止，不跳过检查 |
| 时间 | 744 行服务端 currentTimeMillis 校时 | 尚未接入实时跑步，不以手填日期代替 |
| 人脸 | 833 行开跑配置、1025 行开跑验证、1066–1178 行中途抽检 | 未实现，不能假定所有学校均关闭 |
| 运动 | 583 行后台定位、600 行 motionAnalyzer、858 行位置过滤 | 未实现等价采集，不生成替代数据 |
| 暂停 | 2081 行，单次最多两次 | 无真实场次，不提供伪暂停 |
| 摄像头 | 1628 行点位轮询、1761 行摄像头轮询、1901 行路线配置 | 尚未实现 |
| 提交 | 2112 行 saveScores，先基础记录再详情；详情 success/doFail 均 resolve | 删除旧“详情失败即整次失败”的生成提交链路；没有将真实提交标记为已实现 |
| 补传 | 2224 行 runData 恢复、2274 行结算缓存、RunInfo-* | 尚未实现，真实场次失败重试不等于任意历史补跑 |

成绩列表协议来源于 chunk_98／chunk_100；当前列表可查询，但学期月份筛选、成绩详情、申诉等页面仍未完整复刻。本次完成的是删除不一致的生成提交能力和落实网页开跑限制，不能将整体标注为“与小程序完全一样”。

## 验证结果

2026-09-05：npm test 10 项通过，npm run lint、npm run build 通过。Playwright 桌面 1440px／手机 390px 检查任务、路线、成绩、查询失败、禁用开跑及 Token 登录回归通过。实际本地接口检查 submit=410，bulk／bulk-v2=404。所有业务数据使用模拟响应，没有请求生产服务。
